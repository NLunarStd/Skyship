public interface IInteractable
{
    // for station that player can interact with, e.g. crystal, water ballon station, map table, helm, reactor etc.

    void Interact(UnityEngine.GameObject user);

}
